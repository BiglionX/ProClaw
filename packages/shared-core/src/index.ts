/**
 * @proclaw/shared-core - ProClaw / ProClips 共用核心包
 *
 * Phase 3 将填充：auth / api / db / profile / chat / call / plugins / components / utils / types
 *
 * 当前为占位导出，Phase 1 期间两个 app 各自维持现有 src/services、src/stores 等代码，
 * 不依赖此包。Phase 3 提取共用代码后，两个 app 通过 `@proclaw/shared-core` 别名引用。
 */
export const SHARED_CORE_VERSION = '0.0.0';
