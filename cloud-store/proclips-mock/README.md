# ProClips Mock (cloud-store)

This is a copy of the ProClips mock server for integration/testing inside `cloud-store`.

Run:

```powershell
cd cloud-store\proclips-mock
npm install
npm start
```

The server runs on port `4001` to avoid colliding with the root mock server.
